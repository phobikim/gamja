server:
  port: ${PORT:58080}
  servlet:
    session:
      timeout: 30m              # 세션 유지 시간
      cookie:
        secure: true            # HTTPS에서만 쿠키 전송
        http-only: true         # JS로 쿠키 접근 불가 (XSS 방어)
        same-site: strict       # CSRF 방지: 다른 도메인에서 세션 사용 불가

spring:
  datasource:
    url: jdbc:mariadb://211.208.163.16:13316/gamja
    username: phobikim
    password: phobikim8038!
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
        format_sql: true
