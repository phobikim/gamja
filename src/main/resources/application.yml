server:
  port: 8080

spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/gamja
    username: root
    password: 80383wjd!
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true